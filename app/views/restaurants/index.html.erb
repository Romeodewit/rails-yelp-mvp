<h1>Restaurants</h1>
<%= link_to "Add a restaurant", new_restaurant_path %>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant %> | <%= restaurant.address %> | <%= restaurant.phone_number %> | <%= restaurant.category %></li>
  <% end %>
</ul>
